{
  "name": "henrikwallstrom-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "eleventy --config=eleventy.config.cjs --serve --watch",
    "build": "eleventy --config=eleventy.config.cjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.0",
    "markdown-it": "^14.0.0"
  }
}
